import {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalFooter,
  ModalBody,
  ModalCloseButton,
} from "@chakra-ui/react";

interface deleteModalProps {
  open: boolean;
  setOpen: void;
}

export function delteModal({ open, setOpen }: deleteModalProps) {}
